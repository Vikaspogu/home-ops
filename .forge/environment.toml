# Forge sandbox environment (see forge docs/forge/notes/environment-lockfile.md).
# scripts/kubeconform.sh needs kubeconform + kustomize (helm dirs are skipped).
[tools]
kubeconform = "0.6.7"
kustomize = "5.5.0"
