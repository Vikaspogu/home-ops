---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: cloudflare-dns
  namespace: network
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: cloudflare-dns
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        CF_API_TOKEN: "{{.API_TOKEN_CASA}}"
        CF_ZONE_ID: "{{ .ZONE_ID_CASA }}"
  dataFrom:
    - extract:
        key: cloudflare
