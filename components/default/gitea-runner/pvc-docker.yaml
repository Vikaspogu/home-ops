---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gitea-runner-docker
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 50Gi
