---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gitea-runner-docker
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-block
  resources:
    requests:
      storage: 100Gi
