---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: ivan-kubeconfigs
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: ivan-kubeconfigs
    template:
      engineVersion: v2
      data:
        talos: "{{ .talos_kubeconfig }}"
        omv: "{{ .omv_kubeconfig }}"
  data:
    - secretKey: talos_kubeconfig
      remoteRef:
        key: talos
        property: IVAN_KUBE_CONFIG
    - secretKey: omv_kubeconfig
      remoteRef:
        key: omv
        property: IVAN_KUBE_CONFIG
