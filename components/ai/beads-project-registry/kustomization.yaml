---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ai

configMapGenerator:
  - name: beads-project-registry
    files:
      - projects.json

generatorOptions:
  disableNameSuffixHash: true
