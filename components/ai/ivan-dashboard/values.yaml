controllers:
  app:
    replicas: 1
    annotations:
      reloader.stakater.com/auto: "true"
    containers:
      app:
        image:
          repository: gitea.a113.casa/vpogu/ivan-personal-dashboard
          tag: 20260625233525-eb30ef2-oci
          pullPolicy: Always
        env:
          TZ: America/New_York
          IVAN_SERVICE_UPSTREAM: "http://ivan-personal-service.ai.svc.cluster.local:8080"
        envFrom:
        - secretRef:
            name: ivan-dashboard-secret
        resources:
          requests:
            cpu: 10m
            memory: 32Mi
          limits:
            memory: 128Mi
        probes:
          liveness:
            enabled: true
            custom: true
            spec:
              httpGet:
                path: /healthz
                port: &httpPort 8080
              periodSeconds: 30
              failureThreshold: 3
          readiness:
            enabled: true
            custom: true
            spec:
              httpGet:
                path: /healthz
                port: *httpPort
              periodSeconds: 10
          startup:
            enabled: true
            custom: true
            spec:
              httpGet:
                path: /healthz
                port: *httpPort
              initialDelaySeconds: 3
              periodSeconds: 3
              failureThreshold: 10
        securityContext:
          allowPrivilegeEscalation: false
    pod:
      imagePullSecrets:
      - name: gitea-registry-secret
service:
  app:
    controller: app
    ports:
      http:
        port: *httpPort
