apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: agent-farm
resources:
  - datavolume.yaml
  - presync-recreate-datavolume.yaml
  - golden-image-reconciler.yaml
# ArgoCD Image Updater owns this tag; the image name matches the DataVolume registry URL configured by kustomizeconfig.yaml.
images:
- name: docker://gitea.a113.casa/vpogu/agent-farm-ubuntu
  newTag: 20260917144345-313fd32-oci
configurations:
- kustomizeconfig.yaml
