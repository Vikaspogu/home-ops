apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../../../../components/cert-manager/cert-manager

patches:
  - patch: |
      - op: replace
        path: /spec/target/template/data/api-key
        value: "{{ .api_key }}"
    target:
      kind: ExternalSecret
