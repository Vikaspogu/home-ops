machine:
  kubelet:
    extraConfig:
      # Use tighter image GC than global 65/55 on this 117G CI/agent node to avoid disk pressure.
      imageGCHighThresholdPercent: 55
      imageGCLowThresholdPercent: 45
